var miTexto = document.getElementById("miTexto");

miTexto.addEventListener("click", function () {
  if (miTexto.style.fontWeight === "bold") {
    miTexto.style.fontWeight = "normal";
  } else {
    miTexto.style.fontWeight = "bold";
  }
});

object.onmousemove = function () {
  myScript;
};
